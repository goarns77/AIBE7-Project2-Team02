# 계정·인증 연동 후속 작업 계획 및 명세

## 1. 목적

이 문서는 `AUTH-SELLER-SWAGGER-WORK-SPEC.md`에 정의된 SELLER/JWT/Swagger 패치 이후 남은 계정·인증 관련 작업을 실행 순서대로 정의한다. 사람과 LLM 작업자는 작업 시작 전 현재 브랜치와 변경 상태를 확인하고, 각 작업의 수용 기준과 테스트를 함께 완료해야 한다.

## 2. 기준 상태

- 기준 브랜치: `merge`
- 최초 기준 커밋: `a703720`
- 최신 동기화 기준 커밋: `08c9dac` (`upstream/merge`)
- 회원가입, 이메일 로그인, JWT 발급, 프로필 수정, 소프트 탈퇴는 구현되어 있다.
- 판매자 신청, 관리자 승인/거절, SELLER 승격 및 판매자 URL 권한 패치는 작업 트리에 구현되어 있다.
- Swagger Bearer 인증 및 공개/보호 API 구분 패치는 작업 트리에 구현되어 있다.
- 현재 계정·관리자·마이페이지 관련 파일 중 다수가 미추적 상태이므로 커밋 전 누락 여부를 반드시 확인해야 한다.
- `SwaggerConfig.java` 삭제와 대체 `OpenApiConfig.java` 추가는 하나의 커밋 단위로 처리해야 한다.

## 3. 요구사항 구현 현황

| 요구사항 | 기능 | 상태 | 남은 작업 |
| --- | --- | --- | --- |
| REQ-MEM-01 | 이메일 회원가입 | 완료 | 회귀 테스트 유지 |
| REQ-MEM-02 | 소셜 회원가입 | 미구현 | OAuth 공급자 및 계정 모델 설계 |
| REQ-MEM-03 | 판매자 등록 | 구현 완료, 반영 대기 | 커밋, DB 마이그레이션, 운영 검증 |
| REQ-MEM-04 | 이메일 로그인/JWT | 완료 | 회귀 테스트 유지 |
| REQ-MEM-05 | 소셜 로그인 | 미구현 | 최초 로그인 프로필 완료 흐름 포함 |
| REQ-MEM-06 | 로그아웃 | 완료 | Access Token 단독 정책 유지 및 회귀 테스트 |
| REQ-MEM-07 | 마이페이지 | 부분 구현 | 주문·제안·견적·확정 거래 실제 데이터 연동 |
| REQ-MEM-08 | 소셜 계정 연동 | 미구현 | REQ-MEM-02/05 이후 수행 |
| REQ-MEM-09 | 회원 탈퇴 | 완료 | 운영 데이터 기준 진행 거래 판정 검증 |
| REQ-MEM-10 | 회원 문의 | 미구현 | 문의 도메인/API/UI 구현 |
| REQ-MEM-11 | 신고 | 미구현 | 신고 도메인/API/관리자 UI 구현 |

## 4. 우선순위 요약

| 우선순위 | 작업 ID | 작업 | 이유 |
| --- | --- | --- | --- |
| P0 | AUTH-00 | 현재 패치 원자적 커밋 및 DB 준비 | 파일 누락 시 SELLER/Swagger 기능이 불완전해짐 |
| P0 | AUTH-01 | HTTP 고정 사용자 ID 제거 | 인증된 사용자와 실제 처리 사용자가 불일치함 |
| P0 | AUTH-02 | WebSocket/STOMP JWT 인증 | 구독 및 메시지가 현재 고정 사용자로 처리됨 |
| P0 | AUTH-03 | 공개 페이지 주문 정보 노출 해소 | 매칭 화면에서 타인 주문 정보가 노출될 가능성 |
| P1 | AUTH-04 | 견적·채팅·주문 도메인 계정 검증 연결 | 대상 ID 존재/승인/거래 참여자 검증이 미완성 |
| P1 | AUTH-05 | 마이페이지 실제 활동 데이터 통합 | REQ-MEM-07 핵심 범위가 부분 구현 상태 |
| P1 | AUTH-06 | 로그아웃 및 토큰 수명 정책 확정 | 현재 Access Token 제거만으로는 서버 폐기가 불가함 |
| P2 | AUTH-07 | 회원 탈퇴 거래 제한 및 보존 정책 | 진행 거래 무결성과 개인정보 정책 필요 |
| P2 | AUTH-08 | 소셜 가입·로그인·연동 | 권장 요구사항 REQ-MEM-02/05/08 |
| P3 | AUTH-09 | 회원 문의 | 선택 요구사항 REQ-MEM-10 |
| P3 | AUTH-10 | 신고 및 관리자 처리 | 선택 요구사항 REQ-MEM-11 |

## 5. 단계별 작업 명세

### AUTH-00 현재 패치 반영 안정화

현재 상태: 구현 및 테스트 완료, 커밋·운영 반영 대기

현재 구현:

- `SELLER` 역할, 판매자 신청·승인·거절, 승인 시 역할 승격과 `tokenVersion` 증가를 구현했다.
- 관리자 계정·판매자 신청 관리 API/UI와 역할별 Security matcher를 구현했다.
- 기존 `SwaggerConfig.java`를 `OpenApiConfig.java`로 교체하고 Bearer 인증 및 공개 API 예외를 반영했다.
- 기존 DB 보정을 위한 `db/add-seller-role.sql`을 추가했다.
- 전체 테스트 106건과 `git diff --check`가 통과했으며, 아직 커밋과 운영 DB 적용은 수행하지 않았다.

작업 내용:

- SELLER enum, 승인 승격, JWT 복합 권한, 보안 matcher를 하나의 변경 단위로 반영한다.
- `SwaggerConfig.java` 삭제와 `OpenApiConfig.java` 추가를 반드시 함께 반영한다.
- 관리자 및 마이페이지 관련 미추적 파일의 소유자와 반영 범위를 확인한다.
- 기존 PostgreSQL에는 `src/main/resources/db/add-seller-role.sql`을 적용한다.
- 적용 전후 승인 판매자의 역할 및 `token_version` 보정 건수를 기록한다.

수용 기준:

- 새 체크아웃에서 빌드와 전체 테스트가 통과한다.
- 승인된 판매자는 재로그인 후 SELLER API를 호출할 수 있다.
- 일반 USER의 판매자 전용 API 호출은 403이다.
- `/v3/api-docs`에 `bearerAuth`가 존재하며 로그인과 공개 상품 GET에는 인증 표시가 없다.
- Git에 삭제된 Swagger 설정만 단독 반영되는 상태가 없어야 한다.

### AUTH-01 HTTP 컨트롤러 인증 사용자 연결

현재 상태: 완료

현재 구현:

- 견적·협상·채팅·파일 HTTP 경로가 `@AuthenticationPrincipal Jwt`의 subject를 현재 계정 ID로 사용한다.
- 요청 DTO의 발신자 ID를 제거하거나 무시하고, 서버에서 인증 주체를 행위자로 결정한다.
- 판매자 프로필 ID가 필요한 견적 경로는 계정 ID와 동일하다고 가정하지 않고 명시적으로 변환한다.
- 고정 사용자 ID 반환 코드를 제거하고 비참여자 접근을 `403`으로 매핑했다.

대상:

- `QuoteController`
- `QuoteNegotiationController`
- `ChatController`
- `ChatFileController`
- `ChatMessageController`의 HTTP 조회 경로

작업 내용:

- 모든 `resolveCurrentUserId()` 및 `resolveCurrentSellerId()`의 `return 1L`을 제거한다.
- HTTP 컨트롤러는 `@AuthenticationPrincipal Jwt jwt`의 `subject`를 계정 ID로 사용한다.
- 판매자 ID와 사용자 ID가 별도 키라면 명시적인 조회 서비스를 사용한다. 두 ID가 같다고 가정하지 않는다.
- 요청 DTO의 `senderId`, `buyerId`, `sellerId` 등 클라이언트가 주장하는 행위자 ID는 신뢰하지 않는다.
- 공통 변환이 반복되면 작은 인증 주체 어댑터를 도입하되 전역 정적 유틸리티에 도메인 조회를 넣지 않는다.

수용 기준:

- 코드 검색 결과 인증 경로에 `return 1L`이 남지 않는다.
- 사용자 A의 JWT로 생성한 데이터의 행위자 ID는 A다.
- 사용자 B가 A의 채팅방, 견적, 파일에 접근하면 403 또는 도메인 정책에 맞는 404다.
- 인증 없이 호출하면 401이며 NullPointerException이나 500이 발생하지 않는다.

필수 테스트:

- 컨트롤러 MVC 테스트: JWT subject 전달 검증
- 서비스 테스트: 거래 비참여자 거부 검증
- 통합 테스트: 서로 다른 두 계정으로 교차 접근 검증

### AUTH-02 WebSocket/STOMP JWT 인증

현재 상태: 완료

현재 구현:

- STOMP `CONNECT`의 Bearer JWT를 검증하고 인증 객체를 WebSocket Principal로 설정한다.
- 구독과 메시지 발행 시 채팅방 참여자 여부를 검증하며 payload의 발신자 ID를 신뢰하지 않는다.
- 만료·위조 토큰과 계정 상태·`tokenVersion` 불일치는 공통 JWT 검증 계층에서 거부한다.
- 허용 Origin은 `WEBSOCKET_ALLOWED_ORIGIN_PATTERNS` 환경 변수로 제한하고 기본값은 로컬 개발 Origin만 허용한다.

작업 내용:

- STOMP `CONNECT` 시 Authorization 헤더의 Bearer JWT를 검증하는 `ChannelInterceptor`를 구현한다.
- 검증된 사용자 ID와 권한을 WebSocket `Principal`에 저장한다.
- 메시지 발행과 구독에서는 `Principal`만 사용하고 payload의 sender ID를 신뢰하지 않는다.
- `ChatSubscriptionInterceptor.resolveCurrentUserId(...)`의 고정값을 제거한다.
- 토큰 만료, 위조, 탈퇴/정지, `tokenVersion` 불일치를 연결 단계에서 거부한다.
- 채팅방 참여자가 아닌 사용자의 구독과 발행을 거부한다.

수용 기준:

- 토큰 없는 CONNECT와 위조 토큰 CONNECT는 실패한다.
- 유효한 참여자는 자신의 채팅방을 구독하고 메시지를 보낼 수 있다.
- 비참여자는 채팅방 식별자를 알아도 구독하거나 메시지를 보낼 수 없다.
- 메시지 발신자 ID는 항상 Principal에서 결정된다.

### AUTH-03 페이지 인증 구조 및 매칭 화면 노출 해소

현재 상태: 완료

현재 구현:

- 주문 상세·수정, 제안 작성, 매칭 페이지는 서버 렌더링 시 식별자와 비민감 셸만 전달한다.
- 실제 주문 정보는 JavaScript가 Bearer 토큰으로 보호 API를 호출해 조회한다.
- 주문 상세 API는 구매 요청 소유자 또는 승인 판매자만 접근할 수 있도록 서버에서 검증한다.
- 공개 주문 목록에서는 정확한 배송 주소를 노출하지 않는다.

현재 위험:

- `/requests/**`는 페이지 이동을 위해 `permitAll`이다.
- 신규 `/requests/{requestId}/matches` 페이지 컨트롤러는 인증 없이 서버에서 주문 정보를 조회한다.
- JWT가 localStorage에 있어 일반 브라우저 페이지 이동 요청에는 Authorization 헤더가 자동 포함되지 않는다.

결정할 방식:

1. 권장: 페이지는 민감 데이터를 렌더링하지 않는 정적 셸만 반환하고, 보호 API를 JavaScript Bearer 요청으로 호출한다.
2. 대안: HttpOnly Secure 쿠키 기반 인증을 도입해 서버 렌더링 페이지도 보호한다. CSRF 및 쿠키 수명 정책을 함께 설계해야 한다.

작업 내용:

- `MatchingPageController`에서 주문 객체를 직접 모델에 넣지 않고 식별자만 전달하는 구조를 우선 검토한다.
- 실제 주문 데이터는 소유권을 검증하는 `/api/v1/requests/{requestId}/matches` 또는 별도 보호 API에서 조회한다.
- `/mypage/**`, `/requests/**` 등 공개 페이지가 HTML에 개인정보를 포함하는지 전체 점검한다.

수용 기준:

- 비로그인 사용자가 HTML만 요청해 다른 사용자의 주문 상세를 얻을 수 없다.
- 로그인 사용자는 자신의 주문 매칭만 조회할 수 있다.
- 다른 사용자의 requestId로 API를 호출하면 403 또는 404다.

### AUTH-04 견적·채팅·주문 계정 검증 연결

현재 상태: 보안 검증 범위 완료, 거래 확정 연계는 도메인 후속 작업

현재 구현:

- 견적 생성 시 구매자 계정 상태와 판매자 프로필 승인 상태를 검증한다.
- 견적의 판매자 프로필 ID와 JWT 계정 ID를 변환해 조회·생성 권한을 판정한다.
- 주문 상세, 견적, 협상, 채팅 및 채팅 파일 접근을 소유자나 거래 참여자로 제한한다.
- 권한 부족·리소스 부재·상태 충돌을 각각 `403`·`404`·`409`로 반환하는 거래 API 예외 처리를 추가했다.
- 견적 수락 이후 확정 주문 생성과 정산 완료 모델은 현재 도메인에 없어 후속 거래 도메인 작업으로 남긴다.

작업 내용:

- 견적 대상 구매자의 계정 존재 및 ACTIVE 상태를 검증한다.
- 견적 대상 판매자의 프로필 존재 및 APPROVED 상태를 검증한다.
- 견적·협상·채팅의 조회, 수정, 상태 변경은 거래 참여자에게만 허용한다.
- 견적 ACCEPTED 시 확정 주문 생성 또는 기존 주문 상태 변경 흐름을 연결한다.
- 상태 전이는 중복 수락, 취소 후 수락, 타인의 철회 등을 거부한다.
- 계정 도메인과 타 도메인 사이에는 저장소 직접 참조보다 명시적 조회 포트/서비스를 우선한다.

수용 기준:

- 존재하지 않거나 정지/탈퇴한 대상에게 견적을 생성할 수 없다.
- 승인되지 않은 판매자는 판매자 행위를 수행할 수 없다.
- 거래 비참여자는 리소스 ID를 알아도 내용을 조회하거나 변경할 수 없다.
- 견적 수락과 주문 확정이 트랜잭션 관점에서 일관된다.

### AUTH-05 마이페이지 활동 데이터 통합

현재 상태: 부분 완료

현재 구현:

- 마이페이지 프로필 조회·수정·탈퇴 UI와 JWT 기반 API 호출을 연결했다.
- 내 주문 요청, 제안, 견적, 채팅을 각 도메인 API에 연결하는 어댑터와 역할별 메뉴를 구현했다.
- 구매·판매 확정 내역 API는 현재 거래 도메인에 없어 화면 계약만 준비되어 있으며 실제 목록 연동이 남았다.
- 페이징과 도메인별 응답 계약 통합 및 목록 통합 테스트가 남았다.

대상 데이터:

- 내 주문 요청
- 받은 제안 및 보낸 제안
- 받은 견적 및 보낸 견적
- 진행 중 협상
- 확정 주문 및 완료 거래
- 승인 판매자의 상품 및 판매 관리 메뉴

작업 내용:

- 현재 `mypage-api-adapters.js`의 임시/분산 호출을 실제 API 계약과 맞춘다.
- 마이페이지 집계 API를 만들지, 여러 도메인 API를 조합할지 결정한다.
- 목록의 계정 ID는 JWT subject에서 결정하고 요청 파라미터로 받지 않는다.
- USER와 SELLER의 메뉴 노출뿐 아니라 서버 API 권한도 함께 검증한다.
- 페이징, 정렬, 빈 상태, 오류 상태를 정의한다.

수용 기준:

- 사용자 A는 A의 활동만 조회한다.
- SELLER는 구매 활동과 판매 활동을 모두 볼 수 있다.
- USER에게 판매 관리 UI가 숨겨지고 판매 API도 403이다.
- 프런트 표시와 API 응답 계약에 하드코딩된 샘플 데이터가 없다.

### AUTH-06 로그아웃 및 토큰 정책

현재 상태: 완료

현재 구현:

- 구현 용이성을 우선해 Refresh Token 없이 Access Token 단독 정책을 유지한다.
- 로그아웃 시 브라우저 `sessionStorage`의 `matcheat.accessToken`을 삭제하고 이후 Authorization 헤더 전송을 중단한다.
- 일반 로그아웃은 복제된 Access Token을 서버에서 즉시 폐기할 수 없으며 기본 수명은 1시간임을 정책 문서에 명시했다.
- 계정 정지·탈퇴·판매자 승격은 `tokenVersion`을 증가시켜 기존 JWT를 즉시 거부한다.

결정 사항:

- Access Token 단독 정책을 유지할지 Refresh Token을 도입할지 결정한다.
- 단독 정책이면 로그아웃은 클라이언트 토큰 삭제이며 즉시 서버 폐기가 불가능함을 명시한다.
- Refresh Token 도입 시 저장 방식, 회전, 재사용 탐지, 만료, 로그아웃 폐기를 설계한다.
- 비밀번호 변경, 계정 정지, 탈퇴, 판매자 승격 시 `tokenVersion` 무효화 규칙을 통일한다.

수용 기준:

- 로그아웃 후 브라우저 저장소와 인증 헤더에서 토큰이 제거된다.
- 정책상 즉시 무효화 대상 이벤트 후 기존 토큰이 거부된다.
- 만료와 무효화 오류가 공통 401 응답 형식으로 반환된다.

### AUTH-07 회원 탈퇴 정책 보강

현재 상태: 완료

현재 구현:

- 주문·견적·협상·제안·견적 요청의 진행 상태를 계정 도메인 포트로 조회해 진행 거래가 있으면 탈퇴를 거부한다.
- 진행 거래로 차단된 탈퇴 요청은 `409 ACTIVE_TRANSACTION_EXISTS`를 반환한다.
- 탈퇴는 `WITHDRAWN`, `withdrawn_at`, `tokenVersion` 증가를 적용하는 소프트 삭제로 처리한다.
- 거래 참조는 보존하고 이메일은 현재 고유 제약에 따라 재가입에 사용할 수 없도록 정책을 확정했다.
- 서비스 및 MVC 테스트로 정상 탈퇴와 진행 거래 차단을 검증했다.

작업 내용:

- 진행 중 주문, 협상, 미정산 거래가 있을 때 탈퇴 허용 여부를 정의한다.
- 제한 대상이 있으면 탈퇴 요청을 409 등 일관된 오류로 거부한다.
- `WITHDRAWN`, `withdrawn_at`, `tokenVersion` 처리와 개인정보 마스킹/보존 정책을 문서화한다.
- 탈퇴 계정의 이메일 재가입 허용 시점과 고유 제약 처리 방식을 결정한다.

수용 기준:

- 정책상 진행 거래가 있는 사용자의 탈퇴 결과가 테스트로 고정된다.
- 탈퇴 즉시 기존 JWT가 거부된다.
- 거래 이력의 참조 무결성을 깨지 않고 개인정보 정책을 준수한다.

### AUTH-08 소셜 가입·로그인·계정 연동

선행 조건:

- AUTH-01부터 AUTH-06까지의 인증 주체와 토큰 정책이 안정화되어야 한다.

작업 내용:

- 1차 공급자를 Google 또는 Kakao 중 하나로 제한해 구현한 뒤 확장한다.
- `social_accounts`와 같은 별도 연결 테이블에 provider, provider_user_id, user_id를 저장한다.
- 공급자 식별자 기준 고유 제약을 둔다.
- 최초 로그인 시 필요한 프로필이 부족하면 단기 완료 토큰 또는 서버 세션으로 프로필 설정 화면에 연결한다.
- 기존 이메일 계정과 자동 병합하지 않는다. 재인증된 사용자만 명시적으로 소셜 계정을 연결한다.
- 로그인 성공 후 기존 자체 JWT를 발급해 내부 API 인증 방식을 통일한다.

수용 기준:

- 신규 소셜 사용자는 프로필 완료 후 USER 계정으로 생성된다.
- 기존 연결 사용자는 중복 계정 없이 로그인한다.
- 동일 이메일만으로 계정을 자동 연결하지 않는다.
- 탈퇴/정지 계정과 연결 충돌 시 정책에 맞는 오류를 반환한다.

### AUTH-09 회원 문의

작업 내용:

- 문의 엔티티에 작성자, 제목, 내용, 상태, 답변, 생성/수정 시각을 저장한다.
- 상태는 `RECEIVED`, `IN_PROGRESS`, `COMPLETED`를 사용한다.
- 사용자는 자신의 문의만 등록하고 조회한다.
- 관리자는 전체 문의 조회, 상태 변경, 답변 등록을 수행한다.

수용 기준:

- 타인의 문의는 조회할 수 없다.
- 상태 전이와 관리자 답변 이력이 보존된다.
- 사용자/관리자 API에 각각 권한 테스트가 있다.

### AUTH-10 신고 및 관리자 처리

작업 내용:

- 신고자, 신고 대상 사용자 또는 거래, 사유 코드, 상세 내용, 처리 상태를 저장한다.
- 동일 거래에 대한 중복 신고 정책을 정의한다.
- 관리자는 신고 목록, 상세, 처리 결과를 관리한다.
- 신고 처리와 계정 정지는 별도 명령으로 유지하고 감사 로그를 남긴다.

수용 기준:

- 신고 대상과 거래 참여 관계를 검증한다.
- 일반 사용자는 관리자 처리 정보를 변경할 수 없다.
- 관리자 처리 이력과 계정 상태 변경 주체를 추적할 수 있다.

## 6. 공통 구현 원칙

- 인증 사용자 ID는 JWT subject 또는 검증된 WebSocket Principal에서만 얻는다.
- 클라이언트가 보내는 사용자 ID를 현재 행위자 ID로 사용하지 않는다.
- URL 권한, 역할, 승인 상태, 리소스 소유권은 서로 다른 검증 계층으로 취급한다.
- 401은 인증 실패, 403은 인증됐지만 권한 부족, 404는 노출을 피해야 하는 리소스 부재에 사용한다.
- 새 API를 추가하면 `SecurityConfig`, `OpenApiConfig`, MVC 보안 테스트를 함께 수정한다.
- 기존 사용자 변경을 덮어쓰거나 미추적 파일을 임의로 삭제하지 않는다.
- 수동 SQL은 반복 실행 가능성과 운영 롤백 절차를 검토한다.

## 7. 작업 단위 완료 절차

각 작업 ID는 다음 순서로 종료한다.

1. 시작 전에 `git status --short`와 기준 커밋을 기록한다.
2. 관련 컨트롤러, 서비스, 저장소, 프런트 호출부를 함께 검색한다.
3. 구현 전에 권한 행렬과 실패 HTTP 상태를 테스트로 작성하거나 명세에 확정한다.
4. 대상 테스트를 실행한다.
5. 전체 테스트를 실행한다.
6. `git diff --check`를 실행한다.
7. Swagger `/v3/api-docs`와 실제 Security matcher가 일치하는지 확인한다.
8. 변경 파일, DB 작업, 환경 변수, 남은 위험을 작업 기록에 남긴다.

검증 명령:

```powershell
.\gradlew.bat test
git diff --check
git status --short
```

고정 사용자 ID 잔존 확인:

```powershell
rg -n "return 1L|resolveCurrentUserId|resolveCurrentSellerId" src/main/java
```

## 8. 권장 실행 순서

1. AUTH-00 현재 패치 안정화 및 커밋
2. AUTH-01 HTTP 인증 사용자 연결
3. AUTH-02 WebSocket/STOMP JWT 인증
4. AUTH-03 공개 페이지 정보 노출 해소
5. AUTH-04 견적·채팅·주문 도메인 검증 연결
6. AUTH-05 마이페이지 실제 데이터 통합
7. AUTH-06 로그아웃 및 토큰 정책 확정
8. AUTH-07 회원 탈퇴 정책 보강
9. AUTH-08 소셜 가입·로그인·연동
10. AUTH-09 회원 문의
11. AUTH-10 신고 및 관리자 처리

AUTH-01부터 AUTH-04까지는 보안 및 데이터 무결성 작업이므로 다른 기능 확장보다 먼저 수행한다. AUTH-09와 AUTH-10은 선택 요구사항이므로 일정이 부족하면 후순위로 유지하되, 미구현 상태를 릴리스 명세에 명확히 표시한다.

## 9. 외부 도메인 업데이트 후속 항목

`08c9dac` 상품 기능 업데이트에서 확인했지만 계정·인증 작업 소유 범위가 아닌 항목이다. 상품 도메인 담당 변경으로 처리하고, 계정·인증 패치에서 임의 수정하지 않는다.

### PRODUCT-SEC-01 공개 응답의 계정 ID 제거

- 공개 상품 목록·상세 응답의 `ownerAccountId`가 내부 계정 식별자를 노출한다.
- 공개 DTO에서는 제거하고, UI의 소유자 판별은 보호된 `mine` API 또는 `ownedByCurrentUser` 같은 서버 계산 값으로 대체한다.
- 비로그인 상품 목록·상세 응답에 내부 계정 ID가 없는지 테스트한다.

### PRODUCT-SEC-02 레거시 상품 소유자 보정

- `ownerAccountId IS NULL`인 상품이 소유권 검사를 통과하지 않도록 기본 거부한다.
- 운영 데이터의 소유자를 보정하는 반복 실행 가능한 마이그레이션을 마련한다.
- 소유자 미지정 상품의 수정·삭제는 관리자 외에는 403이어야 한다.

### PRODUCT-SEC-03 관리자 삭제 권한 테스트

- 소유자, 비소유자, 관리자, 일반 사용자별 상품 삭제 권한 테스트를 추가한다.
- JWT role claim은 계정 검증 및 `tokenVersion` 검증을 통과한 인증 객체에서만 사용한다.

### MATCHING-OPS-01 임베딩 호출 운영 정책

- 상품·주문 후보 쌍마다 반복되는 외부 임베딩 호출에 캐시, 배치 또는 기능 플래그를 적용한다.
- `GEMINI_API_KEY` 미설정·할당량 초과·지연 시 경고 로그가 후보 수만큼 쌓이지 않도록 제한한다.
- 외부 호출 실패 시 기존 점수 계산으로 폴백되는 동작과 비용·응답시간 상한을 테스트한다.

### PRODUCT-OPS-01 주소 변환 운영 검증

- 위·경도 직접 입력 제거 후 `KAKAO_REST_API_KEY` 미설정 및 주소 변환 실패 응답을 검증한다.
- `KakaoRouteDistanceService`의 deprecated API 경고를 후속 라이브러리 업그레이드에서 제거한다.

### PAYMENT-SEC-01 결제 인증 및 문서 접근 보정

현재 상태: 후속 브랜치에서 구현 및 테스트 완료, 반영 대기

- 결제·영수증·정산 컨트롤러의 고정 사용자 ID를 제거하고 JWT subject를 사용한다.
- 결제의 구매자 계정 ID와 판매자 프로필 ID를 구분해 계정→판매자 프로필 매핑 후 참여 권한을 판정한다.
- 결제 조회는 거래 참여자, 영수증은 구매자, 정산서는 판매자만 접근하도록 분리한다.
- 결제 권한 오류는 공통 거래 API 예외 처리에서 `403`으로 반환한다.
- 채팅 REST API에 병합 과정에서 추가된 `permitAll`을 제거하고 채팅·결제 보호 경로를 Security/OpenAPI 테스트로 검증한다.

### PAYMENT-DATA-01 결제 멱등성 및 동시성 보강

- `payments.quote_id`에 DB 고유 제약이 없어 같은 견적에 대한 동시 결제 요청이 중복 레코드와 중복 PG 호출을 만들 수 있다.
- 고유 제약, 비관적 잠금 또는 PG idempotency key를 조합해 애플리케이션 조회만이 아닌 DB·PG 경계에서 중복 결제를 차단한다.
- 두 개 이상의 동시 요청을 사용한 통합 테스트로 결제 및 정산 레코드가 각각 한 건만 생성되는지 검증한다.

### SECURITY-OPS-01 CORS 운영 설정 외부화

- 현재 HTTP CORS 허용 Origin은 로컬 개발 주소 세 개로 하드코딩되어 있어 운영 배포 주소를 수용하지 못한다.
- WebSocket Origin 정책과 동일하게 환경 변수 기반 allowlist로 외부화하고, 운영 환경에서 와일드카드를 허용하지 않는다.
- 허용·비허용 Origin 및 preflight 요청 테스트를 추가한다.

### OPENAPI-MAINT-01 보안 정책 단일화

- `OpenApiConfig.isProtectedApi`와 `SecurityConfig`가 공개 API allowlist를 별도로 유지해 신규 API 병합 시 문서와 실제 정책이 어긋날 수 있다.
- 공개 경로 메타데이터를 공유하거나 명시적 OpenAPI security annotation 정책으로 전환한다.
- 최소한 인증·상품 공개·채팅·결제 API의 OpenAPI security와 실제 HTTP 응답을 함께 검증하는 계약 테스트를 유지한다.
